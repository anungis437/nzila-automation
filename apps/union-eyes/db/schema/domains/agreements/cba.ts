/**
 * CBA Schema Alias
 *
 * Kept for compatibility with older imports.
 * Uses the consolidated collective agreements schema.
 */

import { collectiveAgreements } from "./collective-agreements";

export const cba = collectiveAgreements;

