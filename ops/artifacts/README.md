# ops/artifacts/
#
# Deployment artifact manifests generated during CI builds.
# Each file records: artifact digest, SBOM hash, attestation ref, commit SHA.
#
# Used by the deployment promotion model to verify that production
# deploys the same artifact that passed staging validation.
